plugins {
    // lets Gradle auto-download JDK 17 if the machine does not have one
    id("org.gradle.toolchains.foojay-resolver-convention") version "0.8.0"
}

rootProject.name = "fitdeveloper-plugin"
